# Frozen prompt for independent Pro RECERT

Independently audit the supplied SplitAlignerR ENGINE002-FIX001B package.  The
binding prior review is `SAR-PRO-ENGINE002-FIX001A-RECERT-20260804`, defect
`SAR-PRO-E2-FIX001A-001`.  Do not treat Codex's classification, producer and
validator agreement, green job summaries, or regenerated expected values as
proof.

First authenticate all sidecars, safe ZIP inventories, internal manifests,
the complete Git bundle, exact final head/tree, and unsquashed ancestry from
ENGINE001 through FIX001B.  Confirm the certified `v0.1.0` tag still peels to
`17a0927095c7a067817bf598a2556cbe7348a6d0`.  Authenticate the preserved Pro
original FAIL decision, certificate, MRE, output, aggregate checker, and the
three reviewed FIX001A artifacts byte-for-byte.

Independently compile and rerun the exact Pro MRE against the repaired source.
Inspect or rerun the fixed standard vectors, all 34,191 two-part partitions,
and the frozen-seed 10,000 random multipart cases using an independent
standard SHA-256 implementation.  Require zero mismatch and explicitly judge
partial-block preservation, zero-length updates, null-pointer boundaries,
multi-block transitions, repeated digest, and continuation after digest.

Independently parse the corrected golden store, reconstruct the exact
`TruthPlanPayloadAggregate/v1` canonical byte stream, compute standard SHA-256,
and compare footer bytes 48--79.  Confirm the maintained checker does not call
package hashing code or trust builder intermediates.  Confirm the old
defective 906-byte store is rejected and that the aggregate-only tamper is
rejected at the aggregate layer after enclosing non-target hashes are rebuilt.

Authenticate and inspect the final Linux, macOS, Windows, comparison, and
package/Rcpp sanitizer artifacts.  Confirm package build/check/tests,
deterministic golden bytes, all 1,974 authority plans, lifecycle, corruption,
no-replace, Windows binary/u64 behavior, and sanitizer gates.  Separately
judge the designated-host >2 GiB and >4 GiB corrected-store evidence, including
independent standard aggregate reconstruction, fresh-process reopen, and final
record lookup.

Finally confirm that record/store schemas, offsets, domain strings,
length-prefix rules, scientific states, pattern/B* identity, truth/fiber
semantics, public API, package version, release metadata, and certified release
identity are unchanged.  The only intended durable change is the standard
incremental SHA-256 aggregate and enclosing hashes derived from it.

Return commands actually rerun, exact object IDs and artifact hashes, per-gate
results, limitations, and exactly one verdict bound to the exact frozen
FIX001B object:

```text
PASS
FAIL
INCOMPLETE
```

Do not modify source, create a tag/release, or ask another reviewer to replace
the original Pro reviewer.
