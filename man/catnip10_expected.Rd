% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scaffold.R
\name{catnip10_expected}
\alias{catnip10_expected}
\title{Catnip10 benchmark expected oracle output}
\usage{
catnip10_expected(regime = c("global", "local"))
}
\arguments{
\item{regime}{Deletion regime: \code{"global"} (outgroup-first) or \code{"local"}
(locally confined). Partial matching is allowed via \code{\link[=match.arg]{match.arg()}}.}
}
\value{
A \code{data.frame} with 8 rows and 18 columns (\code{gene_id} plus the 17
branch coordinates), all columns character.
}
\description{
Return the deterministic expected gene-by-branch coordinate matrix of the
Catnip10 10-tip coordinate-audit benchmark for one taxon-deletion regime.
Values are kept as character strings so that numeric branch lengths and the
explicit missingness labels (\code{NA_struct}, \code{NA_fuse}) are preserved exactly as
emitted by the independent graph-theoretic oracle. Rows are the full tree
(\verb{*_step0}) followed by seven cumulative deletion steps; columns are the 17
primitive branch coordinates on the unrooted axis (ten terminals \code{t1}--\code{t10}
and seven internals \code{N_12}--\code{N_18}), preceded by \code{gene_id}.
}
\examples{
g <- catnip10_expected("global")
dim(g)
g[["gene_id"]]
}
\seealso{
\link{catnip10_oracle} for the full bundled benchmark object.
}
